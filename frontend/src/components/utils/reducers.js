const reducedUsersFollowing = (following, userId) => {
    following.filter
    
}

    // const reducedFollowing = following.reduce(
    //     (acc, following) => ({
    //         ...acc,
    //         [following.followingId]: following,
    //     }),
    //     {}
    // );